
import React, { HTMLProps } from 'react'

interface Props extends HTMLProps<HTMLAllCollection> {}

export const CARTHOC = (props: Props) => {
    return (
        <div>
            fuck you mother fuckler
        </div>
    )
}